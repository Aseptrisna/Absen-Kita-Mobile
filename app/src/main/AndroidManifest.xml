<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csiindonesia.id">

    <application
        android:allowBackup="true"
        android:icon="@drawable/absen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/absen"
        android:supportsRtl="true"
        android:theme="@style/Theme.AbsenQu">
        <activity
            android:name=".view.absenView"
            android:exported="false" />
        <activity
            android:name=".view.dashboardView"
            android:exported="false" />
        <activity
            android:name=".view.registerView"
            android:exported="false" />
        <activity
            android:name=".view.loginView"
            android:exported="false" />
        <activity
            android:name=".view.splashscreenView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>